import { RiseWindow } from "./PageWindow";

// The page numbers under the grid - the last thing the glide reaches.
export const Pagination: React.FC = () => {
  return (
    <RiseWindow name="Pagination" top={1660} left={592} width={1168} height={32} from={208} />
  );
};
